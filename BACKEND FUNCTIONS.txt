

DECLARATION / INSTANCIATION:
	Window(PApplet sketch, float x, float y, float w, float h)
		create drawspace at the x,y


METHODS:
	Tested: 
		void display()
		void canvasPan(float xo, float yo)
			xo and yo mean (mouseX - pmouseX) and (mouseY - pmouseY) respecitvely
		void zoom(float factor)
		boolean createShape(float x, float y, char shape)
		void setFillColor(int r, int g, int b)
		void setBoarderColor(int r, int g, int b)
		boolean deleteSelected()
		boolean deleteLast()
		boolean restoreLast()
		void clear()

	Untested:
		void reCenter()
		void rotate(float ao)
		void singleSelect(float x, float y)
		void multiSelect(float x, float y)
		boolean copy()
		void paste()


Bugs:
	RestoreLast isn't triggering with my button press? Might just be my keyboard (was testing with just print statements)


TODO:
	Import
	New way to Export
	Pan / Scale Shapes (basis written, have to translate to diff coord (math))
	Textboxes
	Comments
	Grid (have unupploaded code to show it, not to snap)
	setStroke
	Curves
	Triangles
	SetStroke
	Freehand Drawing



